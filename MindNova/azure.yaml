# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: mindnova
metadata:
  template: mindnova-bicep

services:
  api:
    project: src/MindNova.Api
    host: appservice
    language: dotnet

infra:
  provider: bicep
  path: infra
