# ADR NNNN: <short decision title>

**Status:** Proposed | Accepted | Superseded
**Date:** YYYY-MM-DD
**Supersedes:** none | ADR NNNN
**Superseded by:** none | ADR NNNN

## Context

What forces this decision? Describe the problem, the constraints, and the relevant facts about the system as they are *right now*. State load-bearing facts with stable anchors (element names, symbol names, file paths) rather than line numbers. If prior attempts failed, name them by their merged PR number or by description, not by an unmerged or dropped commit hash.

Keep this to what a future reader needs to understand *why* the decision was unavoidable. Link to artefacts; do not paste them.

## Decision

What was decided, stated plainly. Include the concrete shape (a short code or config snippet) when it removes ambiguity. Name the specific files, types, or patterns the decision touches.

## Consequences

**Positive:**

- What gets better or simpler.

**Negative:**

- What gets harder, riskier, or easier to break. Be honest; this is the section future readers trust most.

**Neutral:**

- Side effects that are neither win nor loss but worth recording.

## Alternatives considered

1. **<Alternative name>.** What it was, and why it was rejected. One sentence of "rejected because" is the payload.
2. **<Alternative name>.** ...

## Verification

How does someone confirm a change in this area is correct? Name the manual steps, the test command, or the matrix to walk. Call out the cell or scenario that historically breaks, if any. State what the automated tests do *not* cover.

## References

- `path/to/relevant/file` - what it anchors (use element/symbol names, not line numbers).
- Merged PR numbers that are part of this decision's history (immutable, survive branch deletion).
- External docs, with links.
